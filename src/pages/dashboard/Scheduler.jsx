import React from "react";

const Scheduler = () => {
  return (
    <div
      style={{
        padding: "1rem",
        borderRadius: "1rem",
      }}
    ></div>
  );
};

export default Scheduler;
